// Reserved for future test bootstrap. Phase 4 required ArrayBuffer/SharedArrayBuffer
// polyfills for webidl-conversions when running under Node 18. Since we are moving
// into the next phase with updated tooling, the setup is currently empty but the
// file is kept to preserve the Vitest hook pathway.
export default async function setupTests(): Promise<void> {
    // Intentionally blank.
}
